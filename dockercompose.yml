services:
  ms-buys:
    image: ms-buys:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - ms-network

networks:
  ms-network:
    driver: bridge